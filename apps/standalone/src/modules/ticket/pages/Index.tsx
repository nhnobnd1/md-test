import { PageComponent } from "@moose-desk/core";

interface TicketIndexPageProps {}

const TicketIndexPage: PageComponent<TicketIndexPageProps> = () => {
  return <>TicketIndexPage</>;
};

export default TicketIndexPage;
